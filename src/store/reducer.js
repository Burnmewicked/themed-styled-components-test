const initialState = {
        location: 'Amsterdam',
        weatherData: {}
}

const reducer = (state = initialState, action) => {
switch (action.type) {
    // add cases
    default: 
    return state;
}
};

export default reducer;